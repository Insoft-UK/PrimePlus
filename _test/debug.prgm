#pragma mode( separator(.,;) integer(h64) )
help:=false;

local function()
begin
    local alpha, beta;
    case
        if X==Y then
            X-Y;
        end;
    end;
    repeat
    until X==0;
    while X>=0 do
    end;
end;
function();
